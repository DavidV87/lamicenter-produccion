import { Module } from '@nestjs/common';

/**
 * Módulo Documentos — facturas PDF, planos CutList y parser con fallback manual.
 * Implementación pendiente según orden de bloques del proyecto.
 */
@Module({})
export class DocumentosModulo {}
